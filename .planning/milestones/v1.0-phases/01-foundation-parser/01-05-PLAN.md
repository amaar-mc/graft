---
phase: 01-foundation-parser
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/cli/index.ts
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Running npx graft in any directory discovers files and outputs parse summary"
    - "CLI entry wires discoverFiles to parseFiles and prints results to stderr"
    - "No output goes to stdout (reserved for future MCP JSON-RPC)"
  artifacts:
    - path: "src/cli/index.ts"
      provides: "Functional CLI entry point"
      contains: "discoverFiles"
      min_lines: 20
  key_links:
    - from: "src/cli/index.ts"
      to: "src/indexer/discovery.ts"
      via: "import { discoverFiles }"
      pattern: "discoverFiles"
    - from: "src/cli/index.ts"
      to: "src/parser/index.ts"
      via: "import { parseFiles }"
      pattern: "parseFiles"
---

<objective>
Wire the CLI entry point to connect discoverFiles and parseFiles into a functional pipeline. Running `npx graft` in any directory should discover source files, parse them, and output a summary.

Purpose: Satisfies the Phase 1 goal: "Developers can run `npx graft` in any repo and get structured symbol extraction." Without this, the phase delivers library code but no runnable tool.

Output: `npx graft` produces observable output showing discovered files and parsed symbols.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-parser/01-RESEARCH.md

<interfaces>
From src/indexer/discovery.ts:
```typescript
export async function discoverFiles(rootDir: string, extraIgnorePatterns?: readonly string[]): Promise<string[]>
```

From src/parser/index.ts:
```typescript
export async function parseFiles(filePaths: readonly string[]): Promise<readonly ParseResult[]>
```

From src/logger.ts:
```typescript
export function info(message: string, meta?: Record<string, unknown>): void;
export function error(message: string, meta?: Record<string, unknown>): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CLI entry point</name>
  <files>src/cli/index.ts</files>
  <action>
Replace the placeholder shebang-only `src/cli/index.ts` with a functional entry point.

The CLI should:
1. Keep the `#!/usr/bin/env node` shebang
2. Import `discoverFiles` from `../indexer/discovery`
3. Import `parseFiles` from `../parser/index`
4. Import `info`, `error` from `../logger`
5. Run the pipeline:
   - Call `discoverFiles(process.cwd())`
   - Call `parseFiles(discoveredFiles)`
   - Print summary to stderr via logger: file count, node count, parse time
   - Print a simple formatted output of top-level definitions to stderr (name, kind, file)
6. Handle errors: catch GraftError, print message + hint to stderr, exit 1
7. All output goes to stderr (stdout reserved for MCP in Phase 3)

This is intentionally minimal — Phase 3 adds commander, subcommands, colors, and spinners. This just proves the pipeline works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && pnpm build && node dist/index.cjs 2>&1 | head -5</automated>
  </verify>
  <done>Running the built CLI discovers files in the project directory and outputs a parse summary. The pipeline is wired end-to-end.</done>
</task>

</tasks>

<verification>
- `pnpm build && node dist/index.cjs` — produces output showing discovered files and parsed nodes
- `pnpm tsc --noEmit` — no TypeScript errors
- `pnpm eslint src/cli` — no lint errors
</verification>

<success_criteria>
The CLI entry point connects discovery → parsing → output. Running `npx graft` (or `node dist/index.cjs`) in the project directory produces a summary of discovered files and extracted symbols. The Phase 1 goal is achievable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parser/01-05-SUMMARY.md`
</output>
