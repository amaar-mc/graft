---
phase: 01-foundation-parser
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/parser/index.ts
  - src/parser/tree-sitter.ts
  - src/parser/languages/python.ts
  - src/parser/languages/typescript.ts
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements:
  - QUAL-06

must_haves:
  truths:
    - "pnpm prettier --check src exits 0 (no formatting violations)"
    - "CI pipeline enforces Prettier via format:check step"
    - "All 66 existing tests continue to pass after formatting changes"
  artifacts:
    - path: "src/parser/index.ts"
      provides: "Auto-formatted — no lines exceeding printWidth:100"
    - path: "src/parser/tree-sitter.ts"
      provides: "Auto-formatted — no lines exceeding printWidth:100"
    - path: "src/parser/languages/python.ts"
      provides: "Auto-formatted — no lines exceeding printWidth:100"
    - path: "src/parser/languages/typescript.ts"
      provides: "Auto-formatted — no lines exceeding printWidth:100"
    - path: ".github/workflows/ci.yml"
      provides: "CI step: pnpm format:check after lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pnpm format:check"
      via: "run: step inserted after Lint step"
      pattern: "format:check"
---

<objective>
Close QUAL-06 gap: 4 parser source files have lines exceeding Prettier's printWidth:100, causing `pnpm prettier --check src` to exit 1. CI also has no format:check step, so this violation bypasses the pipeline entirely.

Purpose: QUAL-06 requires ESLint + Prettier both enforced in CI. The code logic is correct — this is a pure formatting enforcement issue.
Output: All source files Prettier-clean, CI enforces format:check on every push/PR.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-format source files with Prettier</name>
  <files>src/parser/index.ts, src/parser/tree-sitter.ts, src/parser/languages/python.ts, src/parser/languages/typescript.ts</files>
  <action>
    From the project root (/Users/amaarchughtai/Developer/projects/graft), run:

      pnpm prettier --write src/parser/index.ts src/parser/tree-sitter.ts src/parser/languages/python.ts src/parser/languages/typescript.ts

    Prettier will reflow lines exceeding printWidth:100 automatically — no manual edits needed. The code logic is not changed; only whitespace and line breaks are adjusted.

    Do NOT run prettier --write on the entire src/ tree — only these 4 files are known to violate the check. Limiting scope prevents accidental changes to files that were already passing.
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && pnpm prettier --check src/parser/index.ts src/parser/tree-sitter.ts src/parser/languages/python.ts src/parser/languages/typescript.ts</automated>
  </verify>
  <done>Command exits 0 with "All matched files use Prettier code style!" — no formatting violations remain in these 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Add format:check step to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Insert a new step into .github/workflows/ci.yml immediately after the "Lint" step and before "Test with coverage":

      - name: Format check
        run: pnpm format:check

    The `format:check` script is already defined in package.json (confirmed in VERIFICATION.md artifact table). No new script needed — just wire it into CI.

    The step order must be: Typecheck → Lint → Format check → Test with coverage → Build.

    Do not change any other part of ci.yml (branches, node version, pnpm version, other steps).
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && grep -n "format:check" .github/workflows/ci.yml</automated>
  </verify>
  <done>grep finds "pnpm format:check" in ci.yml. The step appears between the Lint step and the Test step.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm tests still pass and full Prettier check is clean</name>
  <files></files>
  <action>
    Run the full test suite and the full Prettier check to confirm formatting changes did not introduce regressions:

      cd /Users/amaarchughtai/Developer/projects/graft
      pnpm prettier --check src
      pnpm test

    Both commands must exit 0. If any test fails, inspect whether the Prettier reflow changed any string literals or logic — though this should not happen since Prettier only reformats whitespace and line breaks.
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && pnpm prettier --check src && pnpm test</automated>
  </verify>
  <done>`pnpm prettier --check src` exits 0 (all files clean). `pnpm test` exits 0 (all 66 tests pass). QUAL-06 is fully satisfied.</done>
</task>

</tasks>

<verification>
Run in sequence:

1. `pnpm prettier --check src` — must exit 0
2. `pnpm test` — must exit 0 (all 66 tests pass)
3. `grep "format:check" .github/workflows/ci.yml` — must find the step

QUAL-06 is closed when all three pass.
</verification>

<success_criteria>
- `pnpm prettier --check src` exits 0 — zero formatting violations across all source files
- All 66 tests pass — Prettier reformatting introduced no logic regressions
- `.github/workflows/ci.yml` contains a "Format check" step running `pnpm format:check`
- QUAL-06 requirement is fully satisfied (ESLint + Prettier both enforced, CI pipeline complete)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parser/01-06-SUMMARY.md`
</output>
