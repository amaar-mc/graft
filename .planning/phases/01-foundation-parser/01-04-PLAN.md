---
phase: 01-foundation-parser
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - tests/parser/typescript.test.ts
  - tests/parser/javascript.test.ts
  - tests/parser/python.test.ts
  - tests/integration/stdout.test.ts
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-05
  - PARSE-06
  - INFRA-05

must_haves:
  truths:
    - "TypeScript parser tests verify function, class, method, interface, type, enum extraction"
    - "TypeScript parser tests verify import reference extraction"
    - "TypeScript parser tests verify barrel re-exports and TSX components"
    - "Python parser tests verify function, class, decorator, dataclass extraction"
    - "Python parser tests verify relative import extraction"
    - "All CodeNode objects have correct id format and shape"
    - "stdout contamination test proves zero bytes on stdout during parsing"
  artifacts:
    - path: "tests/parser/typescript.test.ts"
      provides: "TS/JS/TSX parser test suite"
      min_lines: 80
    - path: "tests/parser/python.test.ts"
      provides: "Python parser test suite"
      min_lines: 60
    - path: "tests/integration/stdout.test.ts"
      provides: "stdout contamination integration test"
      min_lines: 20
---

<objective>
Write comprehensive test suites for the parser (TS/JS/TSX + Python) and the stdout contamination integration test. These tests validate all PARSE requirements and INFRA-05.

Purpose: Behavioral verification of the parser pipeline. Every extraction rule, every language construct, and the stdout safety guarantee get tested here.

Output: All parser tests green. Full behavioral coverage of PARSE-01 through PARSE-06 and INFRA-05.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-parser/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TypeScript/JavaScript parser tests</name>
  <files>tests/parser/typescript.test.ts, tests/parser/javascript.test.ts</files>
  <behavior>
    - Parser extracts function, class, method, interface definitions from basic.ts
    - Parser extracts import references from basic.ts
    - Parser extracts type alias, enum, const export definitions (PARSE-04)
    - Parser extracts tsx component definitions from react.tsx
    - Parser extracts barrel re-exports from barrel.ts (PARSE-04)
    - All CodeNode objects have correct id format, kind, filePath, startLine, endLine
    - JavaScript parsing works independently (basic.js)
  </behavior>
  <action>
Create `tests/parser/typescript.test.ts` with describe blocks for:
- "extracts definitions (PARSE-01)" — parse basic.ts, assert finds greet (function), User (interface), UserService (class), getUser (method), UserId (type), Role (enum), MAX_USERS (constant)
- "extracts references (PARSE-02)" — parse basic.ts, assert finds import references
- "TypeScript specific constructs (PARSE-04)" — type aliases, enums, barrel re-exports, decorators
- "TSX support (PARSE-04)" — parse react.tsx, find Dashboard component
- "CodeNode shape (PARSE-06)" — validate id format, kind, startLine <= endLine, absolute filePath

Create `tests/parser/javascript.test.ts` — parse basic.js, verify definitions, references, shape.
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && pnpm vitest run tests/parser/typescript.test.ts tests/parser/javascript.test.ts --reporter=verbose</automated>
  </verify>
  <done>All TS/JS parser tests pass covering definitions, references, TS-specific constructs, TSX, and CodeNode shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Python parser tests</name>
  <files>tests/parser/python.test.ts</files>
  <behavior>
    - Parser extracts function and class definitions from basic.py
    - Parser extracts import references from basic.py
    - Parser extracts decorated functions and dataclasses from classes.py (PARSE-05)
    - Parser extracts relative imports from imports.py (PARSE-05)
    - All CodeNode objects have correct shape (PARSE-06)
  </behavior>
  <action>
Create `tests/parser/python.test.ts` with describe blocks for:
- "extracts definitions (PARSE-03)" — parse basic.py, assert finds greet, UserService, get_user, MAX_USERS
- "extracts references (PARSE-03)" — import references to os.path, json
- "Python specific constructs (PARSE-05)" — decorators, dataclasses, relative imports
- "CodeNode shape (PARSE-06)" — same validation as TS tests
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && pnpm vitest run tests/parser/python.test.ts --reporter=verbose</automated>
  </verify>
  <done>All Python parser tests pass covering definitions, references, decorators, dataclasses, relative imports.</done>
</task>

<task type="auto">
  <name>Task 3: Write stdout contamination integration test</name>
  <files>tests/integration/stdout.test.ts</files>
  <action>
Create `tests/integration/stdout.test.ts` — capture process.stdout.write calls, run parseFiles on fixture files, assert zero bytes written to stdout. This validates INFRA-05 end-to-end.
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/projects/graft && pnpm vitest run tests/integration/stdout.test.ts --reporter=verbose</automated>
  </verify>
  <done>stdout contamination test passes. Parser pipeline produces zero bytes on stdout.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/parser tests/integration --reporter=verbose` — all tests pass
- `pnpm tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
Full behavioral test coverage for the parser pipeline. Every PARSE requirement has at least one test. INFRA-05 stdout safety is proven.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parser/01-04-SUMMARY.md`
</output>
